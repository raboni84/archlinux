{
    "id": "{{timestamp}}",
    "date": "{{env `yearmonth`}}",
    "headless": "true",
    "stage": "userbase",
    "old_stage": "bootstrap",
    "ovf_source": "output/archlinux-{{user `old_stage`}}-console-{{user `date`}}.01-x86_64.ovf",
    "stage_files": "userbase",
    "stage_cmd": "--auto \"\" --no-graphical-env",
    "prov_user": "user",
    "prov_pass": "resu",
    "ovf_name": "../archlinux-{{user `stage`}}-console-{{user `date`}}.01-x86_64",
    "vbox_name": "{{user `id`}}"
}