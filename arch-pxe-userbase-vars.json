{
    "id": "{{timestamp}}",
    "date": "{{isotime `2006.01`}}",
    "iso": "archlinux-{{user `date`}}.01-x86_64.iso",
    "headless": "true",
    "stage": "package",
    "old_stage": "userbase",
    "ova_source": "output/archlinux-{{user `old_stage`}}-desktop-{{user `date`}}.01-x86_64.ova",
    "stage_files": "package",
    "stage_cmd": "--auto /dev/sda \"\"",
    "prov_user": "root",
    "prov_pass": "toor",
    "ova_name": "../archlinux-{{user `stage`}}-desktop-{{user `date`}}.01-x86_64",
    "vbox_name": "{{user `id`}}"
}