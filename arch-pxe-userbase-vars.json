{
    "id": "{{timestamp}}",
    "date": "{{env `yearmonth`}}",
    "iso": "archlinux-{{user `date`}}.01-x86_64.iso",
    "headless": "true",
    "stage": "package",
    "old_stage": "userbase",
    "ovf_source": "output/archlinux-{{user `old_stage`}}-desktop-{{user `date`}}.01-x86_64.ova",
    "stage_files": "package",
    "stage_cmd": "--auto /dev/sda \"\"",
    "prov_user": "root",
    "prov_pass": "toor",
    "ovf_name": "../archlinux-{{user `stage`}}-desktop-{{user `date`}}.01-x86_64",
    "vbox_name": "{{user `id`}}"
}