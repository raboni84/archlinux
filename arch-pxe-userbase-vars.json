{
    "id": "{{timestamp}}",
    "date": "{{env `yearmonthday`}}",
    "iso": "archlinux-{{user `date`}}-x86_64.iso",
    "headless": "true",
    "stage": "package",
    "old_stage": "userbase",
    "ovf_source": "output/archlinux-{{user `old_stage`}}-desktop-{{user `date`}}-x86_64.ovf",
    "stage_files": "package",
    "stage_cmd": "--auto /dev/sda \"atad\"",
    "prov_user": "root",
    "prov_pass": "toor",
    "ovf_name": "../archlinux-{{user `stage`}}-desktop-{{user `date`}}-x86_64",
    "vbox_name": "{{user `id`}}",
    "mem_build": "4096",
    "cpu_build": "4",
    "intnet_name": "localdomain",
    "localmirror": ""
}
