{
    "id": "{{timestamp}}",
    "date": "{{env `yearmonth`}}",
    "iso": "archlinux-{{user `date`}}.01-x86_64.iso",
    "headless": "true",
    "stage": "bootstrap",
    "stage_files": "bootstrap,bootstrap-partition,bootstrap-graphical,bootstrap-skel",
    "stage_cmd": "--auto /dev/sda \"\" toor user resu",
    "prov_user": "root",
    "prov_pass": "toor",
    "ovf_name": "../archlinux-{{user `stage`}}-desktop-{{user `date`}}.01-x86_64",
    "vbox_name": "{{user `id`}}"
}