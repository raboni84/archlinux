{
    "id": "{{timestamp}}",
    "date": "{{env `yearmonthday`}}",
    "headless": "true",
    "stage": "pxeserver",
    "old_stage": "userbase",
    "ovf_source": "output/archlinux-{{user `old_stage`}}-console-{{user `date`}}-x86_64.ovf",
    "opt_encrypt_pwd": "",
    "stage_files": "pxeserver",
    "stage_cmd": "--auto",
    "prov_user": "user",
    "prov_pass": "resu",
    "ovf_name": "../archlinux-{{user `stage`}}-console-{{user `date`}}-x86_64",
    "vbox_name": "{{user `id`}}"
}